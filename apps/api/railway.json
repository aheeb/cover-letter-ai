{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "/mise/shims/python -m venv venv && ./venv/bin/python -m pip install --upgrade pip setuptools build && ./venv/bin/python -m pip install ."
  },
  "deploy": {
    "startCommand": "sh -lc 'echo \"PORT=$PORT\"; exec ./venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port \"$PORT\"'",
    "healthcheckPath": "/healthz"
  }
}
